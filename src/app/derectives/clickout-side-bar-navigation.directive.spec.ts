import { ClickoutSideBarNavigationDirective } from './clickout-side-bar-navigation.directive';

describe('ClickoutSideBarNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickoutSideBarNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
